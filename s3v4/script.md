# S3V4 台本 — MCP②Notion連携　組織の記憶を横断して経営課題を抽出する

- **想定尺**: 約7〜9分（うち実演 約5〜6分）
- **実演**: あり（HTML生成のみ。接続手順はスライドで説明）
- **成果物**: Notion公式MCPで議事録DB・プロジェクト管理DBを横断し、経営課題を抽出したHTMLダッシュボード

---

## [P1] カバー — MCP②Notion連携　組織の記憶を横断して経営課題を抽出する

今回はNotionと連携し、組織に「蓄積された記憶」を横断検索します。

経営者・組織開発・人事など、複数の会議や部門の情報を統合して判断する立場の方にとってとても便利なツールです。

---

## [P2] 情報は蓄積されている。でも、引き出せない。

組織には、議事録、プロジェクトの進捗記録、ナレッジが積み上がっています。

でも、情報は断片的で統合されていません。

今日は、Notion公式のMCPを使ってこの横断検索を実現する方法を実演します。

---

## [P3] 2コマンドでインストール、新しいセッションで即繋がる

接続のセットアップはこのスライドの通りです。2コマンドでインストールして、新しいセッションを開き直すだけで繋がります。

まず `/plugin marketplace add makenotion/claude-code-notion-plugin` でNotionのプラグインマーケットプレイスを追加します。続けて `/plugin install notion-workspace-plugin@notion-plugin-marketplace` でインストールします。

インストール後は必ず新しいセッションを開き直してください。ClaudeはセッションをロードするタイミングでMCPのツール定義を読み込むので、同じセッションのままでは反映されません。

---

## 【実演パート】横断検索と課題抽出

（スライドを閉じて、Claude Code デスクトップ版のターミナルに切り替えます）

## [P4] 2つのデータベースを一度に巡回する

題材は、架空の航空会社「ドレミファ・エアー」の経営会議のための資料作りです。議事録データベースと、プロジェクト管理データベースの2つを横断して情報を収集し、作成します。

---

## [P5] プロンプト

では、Claudeに横断検索を依頼します。

> 来週の経営会議で使う資料を作ってください。Notionの「議事録」データベースから過去3ヶ月の議事録を読み込み、経営課題を洗い出してください。次に「プロジェクト管理」データベースから各プロジェクトの進捗を確認し、課題とプロジェクトを紐づけてHTMLダッシュボードにまとめてください。

（Claudeが複数のデータベースを巡回し始める様子を確認する）

Claudeが両方のデータベースを読み込み、経営課題を整理しています。

（HTMLファイルが生成されるまで待つ）

（生成されたHTMLダッシュボードをブラウザで開いて確認する）

議事録とプロジェクトを横断した経営課題マップが、数分でまとまりました。人手なら数時間かかる作業です。

---

## [P6] 数時間の作業が、数分で終わる

Notion MCPを繋ぐことで、組織に積み上がった議事録・プロジェクト情報・ナレッジを、Claudeが横断的に読み解きます。

これまで人手で数時間かかっていた課題抽出が、数分で終わります。意思決定の速度と質が、同時に上がります。

この動画は以上です。
